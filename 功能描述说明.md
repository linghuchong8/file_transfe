# 批量文件交换服务 功能描述说明

## 一、功能概述

本服务是一套面向银行内部系统的**批量文件交换（Transfer / 数据共享）中间服务**，基于 Spring Boot 构建。
它运行在数据共享平台与行内业务系统之间，负责把上游系统分发过来的文件自动接收、下载、解析、上传到 HDFS，
并在收齐后触发下游系统取数；同时支持向其他系统反向下发文件。

核心能力：

- **消息驱动**：通过 company_three 客户端接口拉取文件通知消息，支持自动回执与手动回执。
- **文件接收入库**：按工作流编码解析文件名，落库并去重（幂等）。
- **文件下载**：支持文件路径方式与文件流方式两种下载模式。
- **文件处理**：对 gz / xml / dat / csv / verf 等格式解压、转换为 txt。
- **HDFS 上传**：将处理后的数据文件上传至 Hadoop 集群（Kerberos 认证）。
- **收齐校验**：按接口 + 日期维度校验文件是否接收完整（固定数量、verf 文件、消息头文件数三种策略）。
- **文件下发**：从 HDFS 取文件回本地，再推送（下发）给其他系统。
- **失败重试与状态复位**：定时重置异常状态，保证断点可续。
- **高可用**：基于 ZooKeeper Leader 选举，多实例部署时只有主节点工作。
- **磁盘清理**：定期删除过期历史文件目录。

## 二、运行环境与技术栈

| 项目 | 说明 |
| --- | --- |
| JDK | 1.8 |
| 构建 | Maven，产物 `com-batch-transfer-company_three-SNAPSHOT.jar` |
| 部署目录 | `/app/app` |
| 启动脚本 | `transfer_service.sh start \| stop \| status \| restart`（另有 `transfer_service_company_three.sh`） |
| 框架 | Spring Boot 2.1.6、Spring Scheduling、MyBatis |
| 存储 | PostgreSQL（`application.properties` 中配置数据源） |
| 大数据 | Hadoop HDFS |
| 调度协调 | ZooKeeper + Curator Leader 选举 |
| 依赖库 | fastjson、gson、guava、commons-io、httpclient/httpmime、commons-vfs2、hadoop-common、nantian cpds_eftp / cpds 消息客户端 |

## 三、代码结构与模块职责

基础包：`com.transfer.psbcx`

| 模块 | 职责 | 主要类 |
| --- | --- | --- |
| `com.transfer.psbcx` | 启动入口 | `TransferTransferServiceApp`（Spring Boot 启动类）、`TransferLocalCheck`（临时测试类） |
| `com.transfer.psbcx.run` | 高可用启动执行器 | `TransferTransferRunner`（Leader 选举，主节点才启动应用） |
| `com.transfer.psbcx.controller` | REST 接口 | `TransferTransferRestController` |
| `com.transfer.psbcx.task` | 定时任务 | `TransferJob`（统一接口）、`TransferReceiveMessageJob`、`TransferDownloadFileJob`、`TransferProcessFileJob`、`TransferSendHdfsJob`、`TransferCheckIsDoneJob`、`TransferDownVerifyFileJob`、`TransferPushJob`、`TransferTryJob`、`TransferCleanUpJob`、`TransferMonitorJob`、`TransferCheckListJob` |
| `com.transfer.psbcx.service` | 业务服务 | `TransferTransferActionService`、`TransferTransferRestActionService`、`TransferHdfsActionService`、`TransferTransferHttpClientService`、`TransferTransferPutService` |
| `com.transfer.psbcx.mapper` | MyBatis Mapper 接口 | `TransferDataShareMessageMapper`、`TransferSendFileInformationMapper`、`TransferVerifyConfigMapper`、`TransferVerifyContentMapper` |
| `com.transfer.psbcx.pojo` | 数据对象 | `TransferDataShareMessage`、`TransferDataVerifyMessage`、`TransferDownloadFileRequest`、`TransferDownloadFileResponse`、`TransferSendFileInformation`、`TransferVerifyConfig`、`TransferVerifyContent` |
| `com.transfer.psbcx.enums` | 枚举 | `TransferDataFileCategory`、`TransferDoneFlag` |
| `com.transfer.psbcx.util` | 工具类 | `TransferAppPropertiesUtil`、`TransferStringHelper`、`TransferFileTools`、`TransferFileEncodingDetector`、`TransferDateTools`、`TransferCryptoUtils`、`TransferXmlTools`、`TransferHdfsFileClient`、`TransferTransferConnection`、`TransferTransferHttpAction`、`TransferSftpTools`、`TransferScpFileTransfer`、`TransferStreamGobbler`、`TransferTransferConstants`、`TransferFlowStatus`、`TransferApiResponse`、`TransferDataShareMessageGenerator` |

## 四、核心业务处理流程

整体数据流：

```
上游系统 / EDB客户端
   │  (消息)
   ▼
TransferReceiveMessageJob ──► pm_data_share_msg 入库（幂等 + 回执）
   │
   ▼
TransferDownloadFileJob ──► 下载文件到本地下载目录
   │
   ▼
TransferProcessFileJob ──► 解压/转 txt
   │
   ▼
TransferSendHdfsJob ──► 上传 HDFS
   │
   ▼
TransferCheckIsDoneJob ──► 判断文件是否收齐（is_done）
   │
   ▼
下游系统取用 / TransferPushJob 反向下发
```

### 1. 接收消息 `TransferReceiveMessageJob`

- 定时（默认 5 秒）拉取消息队列中的文件通知。
- 根据 `is.autoReceiveMessage` 选择自动回执接口或手动回执接口。
- 从消息中解析文件名、发送方系统号、文件个数、消息时间等；文件名按工作流编码规则解析
  （`TransferStringHelper.getWorkflowCode`），得到接口名、行政区划编码、日期等。
- 对部分文件名不含发送方系统码的情况，通过 `interface.filename.syscode.map` 配置映射。
- 文件类型划分：`.check` → `TransferDataFileCategory.CHECK`、`.verf` → `TransferDataFileCategory.VERF`、
  其他 → `TransferDataFileCategory.DATA`；check 文件只回执不入库。
- **幂等处理**：对 `File_id` 去重，避免 company_three 客户端因重平衡重复投递造成重复入库。
- 处理成功后写入 `pm_data_share_msg`，并调用 `ackMessage` 回执。

### 2. 下载文件 `TransferDownloadFileJob` / `TransferTransferRestActionService.download`

- 查询待下载记录（`selectPendingDownload`），先批量将下载状态置为“进行中(02)”，防止重复下载。
- 组装 `TransferDownloadFileRequest`，携带服务名、发送/接收系统号、系统密钥、接收目录、下载方式等。
- 调用 EDB 客户端下载接口：
  - `receiveFile.stream.status=0`：按文件路径方式下载；
  - `=1`：按文件流方式下载（`TransferTransferHttpClientService` 中的流式保存）。
- 结果回写 `downFlag`（成功 `00` / 失败 `03` / 异常待重试 `01`）。

### 3. 处理文件 `TransferProcessFileJob` / `TransferTransferActionService.process`

- 查询待处理记录（`selectPendingProcess`），将处理状态置为“进行中”。
- 按扩展名转换：
  - `gz`：`TransferFileTools.decompress` 解压后按 xml 逻辑转 txt；
  - `xml`：`TransferFileTools.xml2txt`；理财系统走 `xml2txtForFinance`；特殊文件走 `xmlSpectxt`（只取指定字段）；
  - `dat`：`TransferFileTools.dat2txt`（渠道二期格式）；
  - `csv`：`TransferFileTools.csv2txt`；
  - `verf`：逐行解析校验文件内容，写入 `verf_content` 表。
- 字符集由 `TransferFileEncodingDetector` 探测，并可用 `interface.filepath.filecode.map` 手动指定。
- 记录生成文件的字节大小，更新 `processFlag` 与日志。

### 4. 上传 HDFS `TransferSendHdfsJob` / `TransferTransferActionService.sendHdfs`

- 查询待上传记录（`selectPendingHdfs`），置为上传中。
- 通过 `TransferHdfsFileClient`（Kerberos 认证，NameNode HA）上传；目标文件统一改为 `.txt`。
- 空文件（`isDone=4`）无需上传。
- 回写 `sendHdfsFlag` 与 `sendHdfsPath`。

### 5. 收齐校验 `TransferCheckIsDoneJob`

按 `unit_name + tx_date` 分组判断文件是否接收完成，支持三种策略：

1. **固定数量**（`TransferVerifyConfigMapper.selectManyStrategies`）：每个分发日固定次数，达到即完成；否则在配置的零点时间兜底置完成。
2. **verf 文件校验**（`selectVerfStrategies`）：用 verf 文件登记的子文件数量与实际入库数量比对。
3. **消息头文件数校验**（`selectFileNumStrategies`）：用消息头 `file_num` 之和比对。
- 校验通过后把该批次记录 `is_done` 置为已完成，并更新 verf 记录的校验状态。

### 6. 文件下发 `TransferPushJob` / `TransferTransferRestActionService.push`

- 查询待下发记录（`TransferSendFileInformationMapper.selectPendingPush`），置为下发中。
- 从 HDFS 取回文件到本地临时目录，组装上传报文（含接口名、日期、增量标识等），
  调用 EDB 客户端 `uploadFile` 接口完成下发。
- 下发结束删除本地临时文件；失败时累加发送次数并置错误状态。

### 7. 其他任务

| 任务 | 功能 |
| --- | --- |
| `TransferTryJob` | 定时重置下载/处理/上传失败状态（`resetAllDone`、`resetVerifyDone`），便于重试 |
| `TransferCleanUpJob` | 按 `delete.local.data.interval` 删除历史日期目录，释放磁盘 |
| `TransferMonitorJob` | 打印线程池运行状态，便于监控 |
| `TransferCheckListJob` | 探测 EDB 客户端插件名称/版本/状态（定时调度默认关闭） |
| `TransferDownVerifyFileJob` | verf/check 文件下载入口（当前逻辑处于注释保留状态） |

### 8. REST 接口 `TransferTransferRestController`

| 路径 | 方法 | 说明 |
| --- | --- | --- |
| `/transfer/list` | GET | 按条件查询 `pm_data_share_msg` 列表 |
| `/transfer/deleteMsg` | POST | 按条件删除消息记录 |

## 五、高可用与异常恢复

- `TransferTransferRunner` 根据 `is.need.ha` 判断是否启用 HA：
  - 启用时通过 ZooKeeper 选举主节点，仅主节点执行 `TransferTransferServiceApp` 启动逻辑；
  - 与 ZK 连接异常（LOST）时退出进程，避免多主节点同时运行。
- 应用启动完成后（`ApplicationRunner`）调用 `resetAllDownFlag` / `resetVerifyDownFlag`，
  将异常中间态复位，保证主备切换后任务可继续。

## 六、关键数据对象与数据表

| POJO | 对应表 | 说明 |
| --- | --- | --- |
| `TransferDataShareMessage` | `pm_data_share_msg` | 接收到的文件消息及下载/处理/HDFS/完成状态 |
| `TransferVerifyConfig` | `verf_config` | 校验策略配置（verf / filenum / many） |
| `TransferVerifyContent` | `verf_content` | verf 文件解析出的子文件清单（字节数、记录数） |
| `TransferSendFileInformation` | `send_file_info` | 待下发文件信息及下发状态 |
| `TransferDownloadFileRequest` | — | 文件下载请求参数 |
| `TransferDownloadFileResponse` | — | 文件下载响应 |

文件类型枚举 `TransferDataFileCategory`：`VERF("00")`、`CHECK("01")`、`DATA("02")`。

完成标志枚举 `TransferDoneFlag`：未完成 `0`、已完成 `1`、忽略 `2`、已触发 `3`、verf 已校验 `4`。

处理状态常量接口 `TransferFlowStatus`：就绪 `01`、完成 `00`、运行中 `02`、错误 `03`、无需处理 `04`。

## 七、配置文件说明

| 文件 | 说明 |
| --- | --- |
| `resources/application.properties` | Spring Boot 配置：数据源、EDB 接口地址、线程池、定时任务周期 |
| `resources/config.properties` | 业务配置：下载目录、HDFS 前缀、ZooKeeper、接口映射等（`TransferAppPropertiesUtil` 读取） |
| `resources/cpdsmsgj2.properties` | Transfer 消息连接配置 |
| `resources/transfer_app_config.properties` | Transfer 传输服务配置 |
| `resources/logback.xml` | 日志配置 |
| `resources/mapper/*.xml` | MyBatis SQL 映射（与 Mapper 接口一一对应） |

常用可调项：

- `data.share.download.dir.prefix`：本地下载根目录
- `task.upload.local.path.prefix` / `task.process.hdfs.path.prefix`：HDFS 路径前缀
- `scheduled.task.*`：各定时任务的初始延迟与执行周期
- `spring.task.execution.*` / `spring.task.scheduling.*`：线程池与调度线程配置
- `is.need.ha`、`zookeeper.*`：高可用开关与 ZK 参数
- `interface.filepath.filecode.map`、`interface.filename.syscode.map`：文件编码与发送方系统码映射

## 八、部署启动

1. 使用 Maven 打包生成 `com-batch-transfer-company_three-SNAPSHOT.jar`；
2. 上传至服务器 `/app/app` 目录；
3. 执行 `./transfer_service.sh start` 启动，`stop` / `status` / `restart` 分别用于停止、查看状态、重启；
4. 启动脚本通过 crontab 注册守护任务，实现进程异常退出后的自动拉起。
